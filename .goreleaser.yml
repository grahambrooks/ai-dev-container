version: 2

builds:
  - main: .
    binary: devc
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w -X main.version={{.Version}}

archives:
  - format: tar.gz
    name_template: "{{ .ProjectName }}_{{ .Os }}_{{ .Arch }}"

checksum:
  name_template: "checksums.txt"

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^chore:"

brews:
  - repository:
      owner: grahambrooks
      name: homebrew-tap
      token: "{{ .Env.HOMEBREW_TAP_GITHUB_TOKEN }}"
    homepage: "https://github.com/grahambrooks/devc"
    description: "AI-safe development containers with devcontainer.json support"
    license: "MIT"
    install: |
      bin.install "devc"
    test: |
      system "#{bin}/devc", "--help"
    dependencies:
      - name: docker
        type: optional
